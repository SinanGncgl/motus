import { cn } from "@/lib/utils";
import { GraduationCap, LayoutDashboard, Library, Quote, BarChart3, Settings as SettingsIcon } from "lucide-react";
import type { ReactNode } from "react";
import { NavLink } from "react-router";
const NAV_ITEMS=[{to:"/dashboard",label:"Dashboard",icon:LayoutDashboard},{to:"/words",label:"Words",icon:Library},{to:"/practice",label:"Practice",icon:GraduationCap},{to:"/stats",label:"Statistics",icon:BarChart3},{to:"/settings",label:"Settings",icon:SettingsIcon}];
export function AppShell({children}:{children:ReactNode}){return <div className="flex min-h-screen bg-background"><aside className="sticky top-0 hidden h-screen w-64 shrink-0 flex-col border-r border-white/10 bg-card/70 px-4 py-6 lg:flex"><NavLink to="/dashboard" className="flex items-center gap-2.5 px-2"><span className="flex size-8 items-center justify-center rounded-lg bg-primary text-primary-foreground"><Quote className="size-4"/></span><span className="text-[15px] font-semibold tracking-tight">Motus</span></NavLink><p className="px-3 pt-1 text-[11px] uppercase tracking-[0.18em] text-muted-foreground">Private workspace</p><nav className="mt-8 flex flex-col gap-1">{NAV_ITEMS.map((item)=><NavLink key={item.to} to={item.to} className={({isActive})=>cn("flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium text-muted-foreground transition-colors hover:bg-accent hover:text-foreground",isActive&&"bg-accent text-accent-foreground")}>{<item.icon className="size-4"/>}{item.label}</NavLink>)}</nav><div className="mt-auto rounded-xl border border-primary/20 bg-primary/5 p-3"><p className="text-xs font-medium text-primary">Local mode</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Your library and media stay on this machine.</p></div></aside><header className="sticky top-0 z-40 flex h-14 items-center justify-between border-b border-white/10 bg-card/85 px-4 backdrop-blur lg:hidden"><NavLink to="/dashboard" className="flex items-center gap-2"><span className="flex size-7 items-center justify-center rounded-md bg-primary text-primary-foreground"><Quote className="size-3.5"/></span><span className="text-sm font-semibold">Motus</span></NavLink><nav className="flex items-center gap-1">{NAV_ITEMS.map((item)=><NavLink key={item.to} to={item.to} aria-label={item.label} className={({isActive})=>cn("flex size-9 items-center justify-center rounded-lg text-muted-foreground hover:bg-accent",isActive&&"bg-accent text-foreground")}><item.icon className="size-5"/></NavLink>)}</nav></header><main className="min-w-0 flex-1"><div className="mx-auto w-full w-full px-4 py-6 sm:px-6 lg:px-8 lg:py-8">{children}</div></main></div>}
